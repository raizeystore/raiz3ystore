# Supabase

This directory is the source-controlled boundary for the database layer.

## Rule
Schema changes must be represented as migration files in `supabase/migrations/`.

Do not commit Supabase project secrets or service-role credentials.
