# Server-only boundary

Modules in this boundary must never expose secrets to browser code. Use it for trusted server-side operations and integrations.

This marker is documentation only; actual modules should use clear naming and server-only imports where appropriate.
